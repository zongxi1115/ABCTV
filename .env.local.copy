# MoonTV 本地开发环境变量配置

# 站点配置
SITE_NAME=zx TV
ANNOUNCEMENT=本网站仅提供影视信息搜索服务，所有内容均来自第三方网站。本站不存储任何视频资源，不对任何内容的准确性、合法性、完整性负责。

# 存储方式配置
# 可选值: localstorage, redis, d1, upstash
NEXT_PUBLIC_STORAGE_TYPE=localstorage

# 认证配置 (仅在非 localstorage 部署时需要)
USERNAME=admin
PASSWORD=admin123

# Redis 配置 (使用 redis 存储时需要)
# REDIS_URL=redis://localhost:6379

# Upstash Redis 配置 (使用 upstash 存储时需要)
# UPSTASH_URL=
# UPSTASH_TOKEN=

# 是否开放注册 (仅在非 localstorage 部署时生效)
NEXT_PUBLIC_ENABLE_REGISTER=false

# 注册控制开关：true=仅允许登录（禁用注册），false=按配置允许注册
ADMIN_CONTROL=false

# 后台管理员账号（可选）：设置后会自动创建并赋予 admin 权限
# ADMIN_USERNAME=
# ADMIN_PASSWORD=

# 搜索配置
NEXT_PUBLIC_SEARCH_MAX_PAGE=5

# 图片代理配置
# NEXT_PUBLIC_IMAGE_PROXY=

# 豆瓣数据代理配置
# NEXT_PUBLIC_DOUBAN_PROXY=

# 弹幕服务（不配则默认 https://danmu.062679.xyz）
DANMU_API=danmu.062679.xyz

# 内容过滤配置
NEXT_PUBLIC_DISABLE_YELLOW_FILTER=false